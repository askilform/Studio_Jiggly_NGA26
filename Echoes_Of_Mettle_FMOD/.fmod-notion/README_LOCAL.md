# FMOD -> Notion local project helper

This folder was installed by **fmod-notion-ready-kit**.

## FMOD menu

In FMOD Studio:

```text
Scripts -> Reload
Scripts -> Notion -> Export + Sync FMOD Event Registry
```

## Local config

The private config is stored in:

```text
.fmod-notion/local.env
```

Do not commit `local.env`.

## Notion database

Database URL:

```text
https://www.notion.so/785cb6d13fe24dfb8a11b7caf1e476ad
```

Data source ID:

```text
de68321a-16f5-4ea1-9194-7c825c1d2826
```

## Logs

The last FMOD-started sync log is here:

```text
.fmod-notion/logs/last-sync.log
```
