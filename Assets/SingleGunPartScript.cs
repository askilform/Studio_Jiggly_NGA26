using System;
using UnityEngine;

public class SingleGunPartScript : MonoBehaviour
{

    public string partName = "Undefined Gun Part";
    public int partID = 9999;

    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
